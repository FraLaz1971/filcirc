this program plots on the screen
a filled circle with center in 100,100
and radius = 10 (points).
It needs a c language compiler and gnuplot.
The Makefile is created for GNU Make
and gcc compiler, for using other compilers
should be changed accordingly.
usage instructions for GNU/Limux, MacOS and 
MS win msys/mingw
open a terminal and digit

make plot

to remove generated files:

make clean


